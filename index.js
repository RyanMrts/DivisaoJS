

let devidendo, divisor, quociente;

alert ("== DIVISÂO ==")
devidendo = prompt ("Digite o devidendo:")
divisor = prompt ("Digite o divisor: ")

if (divisor == 0)
{
    alert("Não é possível dividir por 0.")
}
else 
{
    resultado = Number(devidendo) / Number(divisor)
    alert (`${devidendo} dividido por ${divisor} é ${resultado}`)
}

Boolean = alert (quociente = Number(devidendo) / Number(divisor))
